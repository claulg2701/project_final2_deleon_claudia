function loadMenu() {
  var myMenu = document.getElementsById('nav-links');

    if ( myMenu.className == 'open') {
      myMenu.className = '';
      myMenu.style.zIndex = '0';
    } else {
      myMenu.className = 'open';
      myMenu.style.zIndex = '1';
    }
}
